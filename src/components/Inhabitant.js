import React from 'react';
import useApiId from '../hooks/useApiId'

export default function Inhabitant({inhabitant}) {
    const inhabitantData = useApiId(inhabitant);
    return (
        <div>
            
        </div>
    )
}
